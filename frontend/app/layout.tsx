import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ 
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter'
})

export const metadata: Metadata = {
  title: 'ecoAI - Carbon Footprint Calculator',
  description: 'GenAI-Powered Climate Action Advisor. Calculate your carbon footprint and get AI-driven sustainability recommendations.',
  keywords: ['carbon footprint', 'sustainability', 'climate action', 'AI', 'environment'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={inter.variable}>
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
